<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:vm="clr-namespace:GamingHub2.MobileApp.ViewModels"
             mc:Ignorable="d"
             x:Class="GamingHub2.MobileApp.Views.LoginPage"
             Shell.NavBarIsVisible="False">
    <ContentPage.BindingContext>
        <vm:LoginVM />
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <StackLayout Padding="10,0,10,0" VerticalOptions="CenterAndExpand" HorizontalOptions="FillAndExpand">
            <Label Text="Korisnicko ime:"/>
            <Entry Text="{Binding Username}"></Entry>
            <Label Text="Lozinka:"/>
            <Entry Text="{Binding Password}"></Entry>
            <Button VerticalOptions="Center" Text="Login" Command="{Binding LoginCommand}"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>